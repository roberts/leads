<div class="onboarding-form__block-group">
    <input
        type="text"
        class="onboarding-form__text-input"
        id="{{ $field->name }}"
    />

    <label
        for="{{ $field->name }}"
        class="onboarding-form__label"
    >
        {{ $field->label }}
    </label>
</div>
